using System;

public class Solution {
    public string solution(string[] arr) {
        string answer = "";
        foreach(var s in arr)
        {
            answer += s;
        }
        return answer;
    }
}